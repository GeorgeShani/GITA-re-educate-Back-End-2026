// Mirrors frontend/src/constants/moods.js's keys exactly (kept manually in
// sync — see API_SPEC.md). Used by both the MoodLog schema's enum and its
// zod validation schema.
export const MOODS = ["veryHappy", "happy", "neutral", "sad", "verySad"];
