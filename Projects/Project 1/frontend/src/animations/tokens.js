// Mirrors the `--ease-standard` / `--duration-*` tokens in src/index.css.
// Framer Motion durations are seconds; CSS durations are ms — keep both in sync.
export const EASE_STANDARD = [0.22, 1, 0.36, 1];

export const DURATION = {
  micro: 0.15,
  button: 0.15,
  card: 0.225,
  dialog: 0.3,
  page: 0.375,
};
